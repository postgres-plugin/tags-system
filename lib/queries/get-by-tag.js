'use strict';

/* eslint-disable */
/*
* We pass getByTag a table that we want get ('challenges') and a tag id
* that we want to filter by. This function should return all of the challenges
* that meet that contain that tag.
*/

// function getByTag (tableName, tagId, cb) {
//   'SELECT tags.name, tags.id, ' + tableName + '.from '
// }
//
// `SELECT tags.id, tags.name, challenges.id, challenges.date, challenges.title,
// challenges.description, challenges.creator_id, challenges.creator_id from tags
// JOIN tags_challenges ON tags.id = 0 AND tags_challenges.tags_id`
